How to Compile and Run (Linux / macOS)

    Save the code to a file named scanner.cpp.

    Open your terminal and compile it using g++:
    Bash

    g++ -o scanner scanner.cpp

    Run the program by passing the target IP and the port range:
    Bash

    ./scanner 127.0.0.1 20 80
